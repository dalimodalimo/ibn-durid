<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>دخول المعلمين - مدرسة ابن دريد</title>
    <link rel="stylesheet" href="/css/teacher_login.css">
</head>
<body>

<div class="login-card">
    <h1>دخول المعلمين</h1>
    
    <% if (error) { %> 
        <div class="error"><%= error %></div> 
    <% } %>
    
    <form action="/teacher/login" method="POST">
        <label for="teacher_id">اسم المعلم:</label>
        <select name="teacher_id" id="teacher_id" required>
            <option value="" disabled selected>-- اختر اسمك من القائمة --</option>
            <% enseignants.forEach(prof => { %>
                <option value="<%= prof.id %>"><%= prof.nom %></option>
            <% }) %>
        </select>

        <label for="password">كلمة المرور:</label>
        <input type="password" name="password" id="password" placeholder="أدخل كلمة المرور" required>

        <button type="submit">فتح الجلسة 🔓</button>
    </form>

    <div class="footer-links">
        <a href="/admin/dashboard">دخول الإدارة (أدمن)</a>
    </div>
</div>

</body>
</html>