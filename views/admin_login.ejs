<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>دخول الأدمن - مدرسة ابن دريد</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        /* --- المتغيرات العامة (نمط الإدارة) --- */
        :root {
            --admin-red: #c0392b;
            --admin-dark-red: #a93226;
            --accent-blue: #3b82f6;
            --bg-dark: #0f172a;
            --text-main: #1e293b;
            --text-muted: #64748b;
        }

        /* --- شاشة التحميل المبتكرة (نفس نمط المعلمين للاتساق) --- */
        #preloader {
            position: fixed;
            inset: 0;
            background: radial-gradient(circle at center, #1e293b 0%, #0f172a 100%);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .innovative-loader {
            position: relative;
            width: 250px;
            height: 250px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .pulse-rings {
            position: absolute;
            border: 2px solid var(--admin-red);
            border-radius: 50%;
            opacity: 0;
            animation: ripple 3s infinite;
        }

        .pulse-rings:nth-child(1) { animation-delay: 0s; }
        .pulse-rings:nth-child(2) { animation-delay: 1s; }

        @keyframes ripple {
            0% { width: 50px; height: 50px; opacity: 0.8; }
            100% { width: 250px; height: 250px; opacity: 0; }
        }

        .user-identity {
            text-align: center;
            z-index: 10;
            background: rgba(15, 23, 42, 0.8);
            padding: 20px;
            border-radius: 50%;
            border: 1px solid var(--admin-red);
            box-shadow: 0 0 30px rgba(192, 57, 43, 0.2);
        }

        .user-name {
            display: block;
            color: #fff;
            font-size: 1.1rem;
            font-weight: 800;
            letter-spacing: 1.5px;
            margin-bottom: 5px;
        }

        .user-phone {
            display: block;
            color: var(--admin-red);
            font-size: 0.85rem;
            font-family: monospace;
            font-weight: bold;
        }

        .hide-loader { opacity: 0; visibility: hidden; transform: scale(1.05); }

        /* --- تنسيق الصفحة الرئيسي (Mobile-First) --- */
        body { 
            font-family: 'Cairo', sans-serif; 
            background: var(--bg-dark); 
            display: flex; 
            flex-direction: column;
            justify-content: center; 
            align-items: center; 
            min-height: 100vh; 
            margin: 0; 
            padding: 15px;
        }

        .login-card { 
            background: #ffffff; 
            padding: 40px 25px; 
            border-radius: 28px; 
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            width: 100%; 
            max-width: 380px; 
            box-sizing: border-box;
            text-align: center;
        }

        .login-header i { font-size: 3rem; color: var(--admin-red); margin-bottom: 15px; }
        .login-header h1 { color: var(--text-main); font-size: 1.6rem; font-weight: 800; margin: 0; }
        .login-header p { color: var(--text-muted); margin-top: 8px; font-size: 0.9rem; }

        input { 
            width: 100%; 
            padding: 16px; 
            margin: 20px 0;
            border: 2px solid #e2e8f0; 
            border-radius: 16px; 
            box-sizing: border-box; 
            font-size: 16px !important;
            text-align: center;
            background: #f8fafc;
            transition: 0.3s;
        }

        input:focus { border-color: var(--admin-red); outline: none; background: #fff; }

        .main-btn { 
            width: 100%; 
            padding: 18px; 
            background: var(--admin-red); 
            color: white; 
            border: none; 
            border-radius: 16px; 
            font-weight: 700; 
            cursor: pointer; 
            font-size: 1.1rem; 
            box-shadow: 0 10px 15px -3px rgba(192, 57, 43, 0.3);
            transition: 0.3s;
        }

        .main-btn:hover { background: var(--admin-dark-red); transform: translateY(-2px); }

        .error { 
            background: #fee2e2; 
            color: #dc2626; 
            padding: 12px; 
            border-radius: 12px; 
            font-size: 0.85rem;
            margin-bottom: 15px;
            border: 1px solid #fecaca;
        }

        /* --- الحقوق --- */
        .copyright-section {
            margin-top: 25px;
            padding-top: 15px;
            border-top: 1px solid #f1f5f9;
        }

        .dev-info { font-size: 0.7rem; color: var(--text-muted); display: flex; justify-content: center; gap: 4px; }
        .dev-name { color: var(--accent-blue); font-weight: 800; }
        .dev-phone { margin-top: 3px; font-size: 0.75rem; color: var(--text-muted); font-family: monospace; font-weight: bold; }

        .back-link {
            display: block;
            margin-top: 20px;
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.85rem;
            transition: 0.3s;
        }
        .back-link:hover { color: var(--accent-blue); }
    </style>
</head>
<body>

<div id="preloader">
    <div class="innovative-loader">
        <div class="pulse-rings"></div>
        <div class="pulse-rings"></div>
        <div class="user-identity">
            <span class="user-name">MOHAMMED ALI</span>
            <span class="user-phone">79737448</span>
        </div>
    </div>
    <p class="loading-status" style="color:#94a3b8; margin-top:20px; font-size:0.8rem;">جاري التحقق من صلاحيات المسؤول...</p>
</div>

<div class="login-card">
    <div class="login-header">
        <i class="fas fa-user-shield"></i>
        <h1>قفل الإدارة</h1>
        <p>لوحة التحكم المركزية - مدرسة ابن دريد</p>
    </div>

    <% if (error) { %> 
        <div class="error"><i class="fas fa-exclamation-circle"></i> <%= error %></div> 
    <% } %>
    
    <form action="/admin/login" method="POST">
        <label style="font-weight: bold; color: var(--text-main);">كلمة مرور المسؤول:</label>
        <input type="password" name="password" placeholder="••••••••" required autofocus>
        <button type="submit" class="main-btn">دخول لوحة التحكم</button>
    </form>
    
    <a href="/teacher/login" class="back-link">
        <i class="fas fa-arrow-right"></i> عودة لدخول المعلمين
    </a>

    <div class="copyright-section">
        <div class="dev-info">
            <span>تم التطوير بواسطة</span>
            <span class="dev-name">MOHAMMED ALI</span>
        </div>
        <div class="dev-phone"><i class="fas fa-phone-alt"></i> 79737448</div>
    </div>
</div>

<script>
    window.addEventListener('load', function() {
        setTimeout(() => {
            document.getElementById('preloader').classList.add('hide-loader');
        }, 1500); 
    });
</script>

</body>
</html>