<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titre %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/reports_style.css">
</head>
<body>
    <div class="container">
        <header class="report-header">
            <h1><i class="fas fa-chart-line"></i> تقارير غياب الطلاب</h1>
            <a href="/admin/dashboard" class="back-link-top">
                <i class="fas fa-home"></i> العودة للوحة التحكم
            </a>
        </header>
        
        <div class="filter-nav">
            <a href="/admin/rapport-absences-eleves" 
               class="<%= (typeof view !== 'undefined' && view === '') ? 'active' : '' %>">
                <i class="fas fa-list"></i> عرض الكل
            </a>
            
            <a href="/admin/rapport-absences-eleves?view=daily" 
               class="<%= (typeof view !== 'undefined' && view === 'daily') ? 'active' : '' %>">
                <i class="fas fa-calendar-day"></i> اليوم
            </a>
            
            <a href="/admin/rapport-absences-eleves?view=weekly" 
               class="<%= (typeof view !== 'undefined' && view === 'weekly') ? 'active' : '' %>">
                <i class="fas fa-calendar-week"></i> آخر أسبوع
            </a>
            
            <a href="/admin/rapport-absences-eleves?view=monthly" 
               class="<%= (typeof view !== 'undefined' && view === 'monthly') ? 'active' : '' %>">
                <i class="fas fa-calendar-alt"></i> هذا الشهر
            </a>

            <button onclick="window.print()" class="btn-print-action">
                <i class="fas fa-print"></i> طباعة التقرير
            </button>
        </div>

        <div class="table-responsive">
            <table>
                <thead>
                    <tr>
                        <th><i class="far fa-calendar-alt"></i> التاريخ</th>
                        <th><i class="far fa-user"></i> اسم الطالب</th>
                        <th><i class="fas fa-school"></i> الفصل / الشعبة</th>
                        <th><i class="far fa-clock"></i> الحصة</th>
                        <th><i class="fas fa-user-check"></i> المعلم الراصد</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (stats.length === 0) { %>
                        <tr>
                            <td colspan="5" class="empty-state">
                                <i class="fas fa-search fa-3x"></i>
                                <p>لم يتم العثور على أي سجلات غياب ضمن هذه الفترة.</p>
                            </td>
                        </tr>
                    <% } else { %>
                        <% stats.forEach(row => { %>
                            <tr>
                                <td style="white-space: nowrap;">
                                    <%= new Date(row.date).toLocaleDateString('ar-EG', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                                </td>
                                <td class="student-name"><%= row.student_name %></td>
                                <td>
                                    <span class="badge-class"><%= row.classe %></span> 
                                    <span class="badge-section">شعبة <%= row.section %></span>
                                </td>
                                <td><span class="badge-periode">الحصة <%= row.periode %></span></td>
                                <td>
                                    <i class="fas fa-chalkboard-teacher" style="font-size: 0.8rem; color: #999;"></i> 
                                    <%= row.teacher_name %>
                                </td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <div class="print-only-footer" style="display: none; margin-top: 30px; text-align: center; border-top: 1px solid #ccc; padding-top: 10px;">
        تم استخراج هذا التقرير من نظام مدرسة ابن دريد الإلكتروني بتاريخ: <%= new Date().toLocaleDateString('ar-EG') %>
    </div>
</body>
</html>