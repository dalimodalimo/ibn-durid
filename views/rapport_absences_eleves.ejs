<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titre %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #f4f7f6; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        .card { border: none; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        .table thead { background-color: #0d6efd; color: white; }
        .status-badge { padding: 5px 12px; border-radius: 20px; font-size: 0.85rem; background-color: #ffc107; color: #000; }
    </style>
</head>
<body>

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold text-primary"><i class="fas fa-user-clock me-2"></i> تقرير غياب الطلاب</h2>
            <p class="text-muted">عرض السجل الكامل لغيابات الطلاب التي تم رصدها بواسطة المعلمين</p>
        </div>
        <a href="/admin/dashboard" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-right me-1"></i> العودة للوحة التحكم
        </a>
    </div>

    <div class="card mb-4">
        <div class="card-body d-flex gap-4">
            <div>
                <span class="text-muted d-block">إجمالي الغيابات المسجلة</span>
                <h3 class="fw-bold m-0"><%= absences.length %></h3>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead>
                        <tr>
                            <th class="ps-4">التاريخ</th>
                            <th>اسم الطالب</th>
                            <th>القسم / الفوج</th>
                            <th>الحصة</th>
                            <th>المعلم الراصد</th>
                            <th class="pe-4">الحالة</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (absences.length > 0) { %>
                            <% absences.forEach(record => { %>
                                <tr>
                                    <td class="ps-4 fw-medium"><%= record.date %></td>
                                    <td><%= record.student_name %></td>
                                    <td>
                                        <span class="badge bg-light text-dark border">
                                            <%= record.classe %> - <%= record.section %>
                                        </span>
                                    </td>
                                    <td>الحصة <%= record.periode %></td>
                                    <td><i class="fas fa-chalkboard-teacher text-muted me-1"></i> <%= record.teacher_name %></td>
                                    <td class="pe-4">
                                        <span class="status-badge">غائب</span>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="6" class="text-center py-5 text-muted">
                                    <i class="fas fa-info-circle d-block mb-2 fs-3"></i>
                                    لا توجد سجلات غياب حالياً
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>