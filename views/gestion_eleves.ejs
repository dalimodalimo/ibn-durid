<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titre %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/settings_style.css">
    <link rel="stylesheet" href="/css/eleves_style.css">
    <style>
        /* --- تحديثات القائمة الجانبية والهيكلية --- */
        :root {
            --sidebar-width: 260px;
            --transition-speed: 0.3s;
        }

        body {
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
            background-color: #f4f7fe;
            margin: 0;
        }

        /* القائمة الجانبية */
        .sidebar {
            width: var(--sidebar-width);
            transition: all var(--transition-speed) cubic-bezier(0.4, 0, 0.2, 1);
            right: 0;
            position: fixed;
            height: 100vh;
            z-index: 1000;
            background: #1e293b; /* لون داكن احترافي */
        }

        /* حالة التصغير */
        .sidebar.collapsed {
            right: calc(var(--sidebar-width) * -1);
        }

        /* المحتوى الرئيسي */
        .main-content {
            flex: 1;
            margin-right: var(--sidebar-width);
            transition: all var(--transition-speed) cubic-bezier(0.4, 0, 0.2, 1);
            width: 100%;
            padding: 20px;
            padding-top: 70px; /* مساحة للزر */
        }

        .main-content.expanded {
            margin-right: 0;
        }

        /* زر التحكم (Toggle Button) */
        .sidebar-toggle {
            position: fixed;
            top: 15px;
            right: 15px;
            z-index: 1100;
            background: #2563eb;
            color: white;
            border: none;
            width: 45px;
            height: 45px;
            border-radius: 10px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
            transition: 0.3s;
        }

        .sidebar-toggle:hover {
            background: #1d4ed8;
            transform: scale(1.05);
        }

        /* تحسينات الوسوم والجداول */
        .badge-blue {
            background: #dbeafe;
            color: #1e40af;
            padding: 4px 12px;
            border-radius: 6px;
            font-weight: bold;
        }

        @media (max-width: 768px) {
            .sidebar {
                right: calc(var(--sidebar-width) * -1);
            }
            .sidebar.active {
                right: 0;
            }
            .main-content {
                margin-right: 0;
            }
        }
    </style>
</head>
<body>

<button class="sidebar-toggle" id="toggleSidebarBtn" title="تبديل القائمة">
    <i class="fas fa-bars"></i>
</button>

<div class="sidebar" id="mainSidebar">
    <div class="sidebar-header">
        <h2><i class="fas fa-user-graduate"></i> شؤون الطلاب</h2>
    </div>

    <div class="sidebar-nav">
        <div class="nav-label">الأساسيات</div>
        <a href="/admin/dashboard"><i class="fas fa-home"></i> العودة للرئيسية</a>
        
        <div class="nav-label">العمليات</div>
        <a href="#section-stats"><i class="fas fa-chart-pie"></i> الإحصائيات</a>
        <a href="#section-add-student"><i class="fas fa-user-plus"></i> تسجيل طالب</a>

        <div class="nav-label">البيانات</div>
        <a href="#section-list"><i class="fas fa-users"></i> قائمة الطلاب</a>
        <a href="/admin/settings"><i class="fas fa-cog"></i> إعدادات النظام</a>
    </div>
</div>

<div class="main-content" id="mainBody">
    <header class="page-header">
        <h1><i class="fas fa-users"></i> إدارة سجلات الطلاب</h1>
    </header>

    <div class="stats-grid" id="section-stats">
        <div class="stat-card">
            <i class="fas fa-user-graduate"></i>
            <div class="stat-info">
                <h3><%= eleves.length %></h3>
                <p>إجمالي الطلاب</p>
            </div>
        </div>
        <div class="stat-card" style="border-right-color: #27ae60;">
            <i class="fas fa-school"></i>
            <div class="stat-info">
                <h3><%= classes.length %></h3>
                <p>عدد الصفوف</p>
            </div>
        </div>
    </div>

    <section class="card" id="section-add-student">
        <h3><i class="fas fa-plus-circle" style="color: #27ae60;"></i> تسجيل طالب جديد</h3>
        <form action="/admin/eleves/ajouter" method="POST" class="student-form-grid">
            <div class="input-group">
                <label>الاسم الكامل للطالب</label>
                <input type="text" name="nom" placeholder="أدخل اسم الطالب رباعي" required>
            </div>
            
            <div class="input-group">
                <label>الصف والشعبة</label>
                <select name="class_info" required>
                    <option value="" disabled selected>-- اختر الوجهة --</option>
                    <% classes.forEach(c => { %>
                        <% for(let i=1; i <= c.num_sections; i++) { %>
                            <option value="<%= c.class_name %>|<%= i %>">
                                <%= c.class_name %> - شعبة <%= i %>
                            </option>
                        <% } %>
                    <% }) %>
                </select>
            </div>

            <button type="submit" class="btn btn-save" style="width: auto; margin-top: 25px;">
                <i class="fas fa-check-double"></i> تأكيد التسجيل
            </button>
        </form>
    </section>

    <section class="card" id="section-list">
        <div class="card-header-flex">
            <h3><i class="fas fa-list-ul"></i> سجل الطلاب المقيّدين</h3>
            
            <div class="filter-tools">
                <input type="text" id="searchInput" onkeyup="filterTable()" placeholder="🔍 ابحث بالاسم...">
                <select id="classFilter" onchange="filterTable()">
                    <option value="">كل الصفوف</option>
                    <% classes.forEach(c => { %>
                        <option value="<%= c.class_name %>"><%= c.class_name %></option>
                    <% }) %>
                </select>
            </div>
        </div>

        <div class="table-responsive">
            <table id="studentsTable">
                <thead>
                    <tr>
                        <th>اسم الطالب</th>
                        <th>الصف</th>
                        <th>الشعبة</th>
                        <th>الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (eleves.length === 0) { %>
                        <tr>
                            <td colspan="4" class="empty-state">
                                <i class="fas fa-folder-open fa-3x"></i>
                                <p>لا توجد بيانات مسجلة حالياً.</p>
                            </td>
                        </tr>
                    <% } else { %>
                        <% eleves.forEach(student => { %>
                        <tr>
                            <td class="student-name"><%= student.nom %></td>
                            <td><span class="badge-blue"><%= student.classe %></span></td>
                            <td><span class="section-text">الشعبة <%= student.section %></span></td>
                            <td>
                                <a href="/admin/eleves/supprimer/<%= student.id %>" 
                                   class="btn-delete-link" 
                                   onclick="return confirm('حذف الطالب <%= student.nom %>؟')">
                                    <i class="fas fa-trash-alt"></i> حذف
                                </a>
                            </td>
                        </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </section>
</div>

<script>
    // 1. نظام إخفاء وإظهار القائمة
    const toggleBtn = document.getElementById('toggleSidebarBtn');
    const sidebar = document.getElementById('mainSidebar');
    const mainBody = document.getElementById('mainBody');

    toggleBtn.addEventListener('click', () => {
        if (window.innerWidth <= 768) {
            sidebar.classList.toggle('active');
        } else {
            sidebar.classList.toggle('collapsed');
            mainBody.classList.toggle('expanded');
        }
        
        // تغيير أيقونة الزر للجمالية
        const icon = toggleBtn.querySelector('i');
        icon.classList.toggle('fa-bars');
        icon.classList.toggle('fa-times');
    });

    // إغلاق القائمة عند النقر في الخارج (وضع الجوال)
    document.addEventListener('click', (e) => {
        if (window.innerWidth <= 768 && !sidebar.contains(e.target) && !toggleBtn.contains(e.target)) {
            sidebar.classList.remove('active');
        }
    });

    // 2. نظام فلترة البحث والصفوف (كودك الأصلي)
    function filterTable() {
        const searchText = document.getElementById('searchInput').value.toLowerCase();
        const classFilter = document.getElementById('classFilter').value;
        const rows = document.querySelectorAll('#studentsTable tbody tr');

        rows.forEach(row => {
            const nameCell = row.cells[0];
            const classCell = row.cells[1];
            if (nameCell) {
                const nameText = nameCell.textContent.toLowerCase();
                const classText = classCell.textContent;
                const matchesSearch = nameText.includes(searchText);
                const matchesClass = classFilter === "" || classText === classFilter;
                row.style.display = (matchesSearch && matchesClass) ? "" : "none";
            }
        });
    }
</script>

</body>
</html>