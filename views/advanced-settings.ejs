<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إعدادات النظام المتقدمة - <%= ecole %></title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        :root {
            --sidebar-width: 280px; 
            --primary-dark: #0f172a; 
            --accent-gold: #fbbf24;
            --soft-blue: #3b82f6; 
            --bg-slate: #f8fafc; 
            --card-bg: #ffffff;
            --text-main: #1e293b; 
            --text-muted: #64748b; 
            --border-color: #e2e8f0;
            --danger-red: #ef4444;
            --success-green: #10b981;
            --card-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }

        [data-theme="dark"] {
            --bg-slate: #0b0f1a;
            --card-bg: #161e2d;
            --text-main: #f1f5f9;
            --text-muted: #94a3b8;
            --border-color: #1e293b;
        }

        body { 
            font-family: 'Cairo', sans-serif; 
            background: var(--bg-slate); 
            color: var(--text-main); 
            margin: 0; 
            padding: 40px;
            line-height: 1.6;
        }
        
        header { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--border-color);
        }

        .header-title h1 { margin: 0; font-size: 1.8rem; font-weight: 800; }
        .header-title p { margin: 5px 0 0; color: var(--text-muted); }

        .danger-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); 
            gap: 25px; 
        }

        .danger-card {
            background: var(--card-bg); 
            padding: 30px; 
            border-radius: 24px;
            box-shadow: var(--card-shadow);
            border: 1px solid var(--border-color); 
            position: relative;
            overflow: hidden;
        }

        .danger-card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; height: 4px;
            background: var(--danger-red);
        }

        .backup-card::before { background: var(--success-green); }

        .card-icon {
            width: 50px; height: 50px; border-radius: 12px;
            display: flex; align-items: center; justify-content: center;
            font-size: 1.5rem; margin-bottom: 20px;
        }

        .danger-card h3 { margin: 0 0 10px; font-weight: 800; }
        .danger-card p { font-size: 0.9rem; color: var(--text-muted); margin-bottom: 25px; }

        .btn-action {
            width: 100%; padding: 14px; margin-top: 12px; border-radius: 12px;
            border: 1.5px solid var(--danger-red); background: transparent; 
            color: var(--danger-red); font-family: inherit;
            font-weight: 700; cursor: pointer; transition: all 0.3s ease;
            display: flex; align-items: center; justify-content: center; gap: 10px;
        }

        .btn-action:hover { background: var(--danger-red); color: white; transform: translateY(-2px); }
        
        .btn-backup { 
            border-color: var(--success-green); 
            color: var(--success-green); 
        }

        .btn-backup:hover { background: var(--success-green); color: white; }

        .btn-back {
            background: var(--card-bg); border: 1px solid var(--border-color);
            padding: 10px 20px; border-radius: 10px; cursor: pointer;
            color: var(--text-main); font-weight: 600; font-family: inherit;
            display: flex; align-items: center; gap: 8px; transition: 0.3s;
        }

        .btn-back:hover { background: var(--primary-dark); color: white; }

        .warning-box {
            background: rgba(239, 68, 68, 0.1); color: var(--danger-red);
            padding: 15px; border-radius: 12px; font-size: 0.8rem;
            margin-bottom: 20px; border-right: 4px solid var(--danger-red);
        }
    </style>
</head>
<body data-theme="light">

    <header>
        <div class="header-title">
            <h1><i class="fas fa-user-shield" style="color:var(--danger-red);"></i> التحكم المتقدم بالنظام</h1>
            <p>إدارة قواعد البيانات، العمليات الحساسة، والنسخ الاحتياطي</p>
        </div>
        <button onclick="location.href='/admin/dashboard'" class="btn-back">
            <i class="fas fa-arrow-right"></i> العودة للوحة التحكم
        </button>
    </header>

    <div class="danger-grid">
        <div class="danger-card">
            <div class="card-icon" style="background:rgba(239,68,68,0.1); color:var(--danger-red);">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h3>إعادة ضبط البيانات (Reset)</h3>
            <div class="warning-box">
                <i class="fas fa-info-circle"></i> تنبيه: عمليات الحذف هنا نهائية ولا يمكن استرجاع البيانات بعد تنفيذها.
            </div>
            
            <button class="btn-action" onclick="confirmReset('eleves', 'حذف جميع الطلاب وسجلاتهم')">
                <i class="fas fa-users-slash"></i> مسح قاعدة بيانات الطلاب
            </button>
            <button class="btn-action" onclick="confirmReset('absences', 'تصفير كافة سجلات الغياب')">
                <i class="fas fa-user-clock"></i> تصفير غياب الطلاب والمعلمين
            </button>
            <button class="btn-action" onclick="confirmReset('timetable', 'حذف الجدول المدرسي الحالي')">
                <i class="fas fa-calendar-times"></i> مسح الجداول الدراسية
            </button>
        </div>

        <div class="danger-card backup-card">
            <div class="card-icon" style="background:rgba(16,185,129,0.1); color:var(--success-green);">
                <i class="fas fa-database"></i>
            </div>
            <h3>الأرشفة والنسخ الاحتياطي</h3>
            <p>يُنصح دائماً بتحميل نسخة احتياطية من البيانات قبل إجراء أي تعديلات كبرى أو في نهاية كل فصل دراسي.</p>
            
            <button class="btn-action btn-backup" onclick="location.href='/admin/export-db'">
                <i class="fas fa-file-download"></i> تصدير قاعدة البيانات كاملة (SQL)
            </button>
            <button class="btn-action btn-backup" onclick="location.href='/admin/export-excel'">
                <i class="fas fa-file-excel"></i> تصدير كشوفات الطلاب (Excel)
            </button>
            <button class="btn-action btn-backup" style="border-style: dashed; margin-top: 20px;">
                <i class="fas fa-history"></i> سجل العمليات المتقدمة
            </button>
        </div>
    </div>

    <script>
        // التحقق من الثيم المفضل
        if (localStorage.getItem('theme') === 'dark') {
            document.body.setAttribute('data-theme', 'dark');
        }

        function confirmReset(target, label) {
            Swal.fire({
                title: 'تأكيد الحذف النهائي؟',
                html: `أنت على وشك القيام بـ <b>(${label})</b>.<br>سيتم مسح البيانات من الجداول المرتبطة أيضاً.`,
                icon: 'error',
                showCancelButton: true,
                confirmButtonColor: '#ef4444',
                cancelButtonColor: '#64748b',
                confirmButtonText: 'نعم، احذف الآن',
                cancelButtonText: 'تراجع',
                reverseButtons: true
            }).then((result) => {
                if (result.isConfirmed) {
                    // إظهار مؤشر تحميل قبل التحويل
                    Swal.fire({
                        title: 'جاري المعالجة...',
                        allowOutsideClick: false,
                        didOpen: () => { Swal.showLoading(); }
                    });
                    window.location.href = `/admin/reset/${target}`;
                }
            });
        }
    </script>
</body>
</html>