<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>دخول الإدارة - مدرسة ابن دريد</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --admin-primary: #c0392b; /* أحمر إداري */
            --admin-dark: #922b21;
            --bg-dark-slate: #1e293b;
            --text-main: #0f172a;
            --text-muted: #64748b;
        }

        body { 
            font-family: 'Cairo', sans-serif; 
            background: var(--bg-dark-slate); /* خلفية داكنة لتمييز منطقة الإدارة */
            display: flex; 
            flex-direction: column;
            justify-content: center; 
            align-items: center; 
            min-height: 100vh; 
            margin: 0; 
        }

        .login-card { 
            background: white; 
            padding: 45px 40px; 
            border-radius: 24px; 
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            width: 100%; 
            max-width: 400px; 
            border-top: 5px solid var(--admin-primary); /* خط علوي لتمييز رتبة الصفحة */
        }

        .login-header { text-align: center; margin-bottom: 35px; }
        .login-header .icon-badge { 
            background: #fdf2f2;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: var(--admin-primary);
            font-size: 2.5rem;
        }
        .login-header h1 { color: var(--text-main); font-size: 1.6rem; font-weight: 800; margin: 0; }
        .login-header p { color: var(--text-muted); margin-top: 8px; font-size: 0.9rem; letter-spacing: 0.5px; }

        .input-group { margin-bottom: 25px; }
        label { display: block; margin-bottom: 12px; font-weight: 700; color: var(--text-main); font-size: 0.95rem; }
        
        input { 
            width: 100%; 
            padding: 15px; 
            border: 2px solid #e2e8f0; 
            border-radius: 12px; 
            box-sizing: border-box; 
            font-family: inherit; 
            font-size: 1.1rem;
            text-align: center;
            transition: all 0.2s ease;
            letter-spacing: 4px; /* لجعل النجوم متباعدة في كلمة المرور */
            background: #fcfcfc;
        }

        input:focus { 
            border-color: var(--admin-primary); 
            background: white;
            outline: none; 
            box-shadow: 0 0 0 4px rgba(192, 57, 43, 0.1); 
        }

        .admin-btn { 
            width: 100%; 
            padding: 16px; 
            background: var(--admin-primary); 
            color: white; 
            border: none; 
            border-radius: 12px; 
            font-weight: 800; 
            cursor: pointer; 
            font-size: 1.1rem; 
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 12px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(192, 57, 43, 0.3);
        }

        .admin-btn:hover { 
            background: var(--admin-dark); 
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(192, 57, 43, 0.4);
        }

        .back-footer {
            margin-top: 30px;
            text-align: center;
        }

        .back-link {
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: color 0.2s;
        }

        .back-link:hover {
            color: #1e3a8a; /* العودة للون المعلمين عند التحويم */
        }

        .error { 
            background: #fef2f2; 
            color: #b91c1c; 
            padding: 14px; 
            border-radius: 10px; 
            text-align: center; 
            margin-bottom: 25px; 
            font-size: 0.9rem;
            border-right: 4px solid #b91c1c;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
    </style>
</head>
<body>

<div class="login-card">
    <div class="login-header">
        <div class="icon-badge">
            <i class="fas fa-user-shield"></i>
        </div>
        <h1>بوابة المسؤول</h1>
        <p>نظام الإدارة المركزي - مدرسة ابن دريد</p>
    </div>

    <% if (error) { %>
        <div class="error">
            <i class="fas fa-lock"></i>
            <span><%= error %></span>
        </div>
    <% } %>

    <form action="/admin/login" method="POST">
        <div class="input-group">
            <label>كلمة مرور النظام:</label>
            <input type="password" name="password" placeholder="••••••••" required autofocus>
        </div>

        <button type="submit" class="admin-btn">
            <span>تأكيد الهوية</span>
            <i class="fas fa-shield-alt"></i>
        </button>
    </form>

    <div class="back-footer">
        <a href="/teacher/login" class="back-link">
            <i class="fas fa-chevron-right"></i>
            العودة لبوابة المعلمين
        </a>
    </div>
</div>

</body>
</html>