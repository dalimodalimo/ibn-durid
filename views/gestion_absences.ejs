<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إدارة حصص الاحتياط - مدرسة ابن دريد</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        :root {
            --sidebar-width: 260px;
            --transition-speed: 0.3s;
            --primary-color: #1e293b;
            --success-color: #10b981;
            --accent-color: #3b82f6;
            --info-bg: #e0f2fe;
            --info-border: #0284c7;
            --warning-color: #f59e0b;
            --danger-color: #ef4444;
        }

        body {
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
            background-color: #f4f7fe;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .sidebar {
            width: var(--sidebar-width);
            transition: all var(--transition-speed) ease;
            right: 0;
            position: fixed;
            height: 100vh;
            z-index: 2000;
            background: var(--primary-color);
            box-shadow: -2px 0 10px rgba(0,0,0,0.1);
        }

        .sidebar.collapsed {
            right: calc(var(--sidebar-width) * -1);
        }

        .main-content {
            flex: 1;
            margin-right: var(--sidebar-width);
            transition: all var(--transition-speed) ease;
            width: 100%;
            padding: 20px;
            padding-top: 80px;
        }

        .main-content.expanded {
            margin-right: 0;
        }

        .sidebar-toggle {
            position: fixed;
            top: 15px;
            right: 15px;
            z-index: 2100;
            background: var(--primary-color);
            color: white;
            border: none;
            width: 45px;
            height: 45px;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        /* تنسيقات بطاقات التغطية المحدثة */
        .recap-banner {
            background: var(--info-bg); 
            border-right: 5px solid var(--info-border);
            padding: 20px; 
            border-radius: 12px; 
            margin-bottom: 25px;
        }

        .recap-grid {
            display: grid; 
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 15px; 
            margin-top: 15px;
        }

        .recap-item {
            background: white; 
            padding: 15px; 
            border-radius: 12px;
            display: flex; 
            align-items: center; 
            justify-content: space-between;
            border: 1px solid #e2e8f0;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }

        .badge-periode {
            background: var(--primary-color); 
            color: white;
            padding: 4px 10px; 
            border-radius: 6px; 
            font-weight: bold;
            font-size: 0.85rem;
        }

        .btn-retirer {
            background: none; 
            border: none; 
            color: var(--danger-color);
            cursor: pointer; 
            padding: 5px; 
            font-size: 1.2rem;
            transition: 0.2s;
        }
        .btn-retirer:hover { transform: scale(1.1); color: #b91c1c; }

        .arrow-icon {
            color: var(--success-color);
            font-size: 1.2rem;
            margin: 0 15px;
        }

        .absent-item-card {
            background: white; border-radius: 12px; padding: 15px;
            margin-bottom: 15px; box-shadow: 0 2px 8px rgba(0,0,0,0.05);
            border-right: 5px solid var(--warning-color);
        }

        .distribution-options {
            display: flex; gap: 10px; margin-top: 12px;
            padding-top: 12px; border-top: 1px solid #f1f5f9;
        }

        .btn-auto {
            background-color: var(--success-color); color: white; border: none;
            padding: 8px 16px; border-radius: 6px; cursor: pointer; font-size: 0.85rem;
        }

        .btn-manual {
            background-color: white; color: var(--accent-color);
            border: 1px solid var(--accent-color); padding: 8px 16px;
            border-radius: 6px; cursor: pointer; font-size: 0.85rem;
        }

        .manual-suggestions-table {
            width: 100%; margin-top: 10px; border-collapse: collapse; display: none;
            background: #f8fafc; border-radius: 8px; overflow: hidden;
        }

        @media (max-width: 768px) {
            .sidebar { right: calc(var(--sidebar-width) * -1); }
            .sidebar.active { right: 0; }
            .main-content { margin-right: 0 !important; }
            .recap-grid { grid-template-columns: 1fr; }
        }

        /* تحسين حاوية البطاقة العامة */
        .card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            margin-bottom: 25px;
            border: 1px solid #edf2f7;
        }

        .card h3 {
            color: var(--primary-color);
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.2rem;
        }

        /* تنسيق نموذج تسجيل الغياب الجديد */
        .form-abs-grid {
            display: grid;
            grid-template-columns: 1fr 1.5fr auto; /* توزيع الأعمدة */
            gap: 15px;
            align-items: center;
            background: #f8fafc;
            padding: 20px;
            border-radius: 12px;
        }

        .input-group-custom {
            display: flex;
            flex-direction: column;
            gap: 5px;
        }

        .input-group-custom label {
            font-size: 0.85rem;
            font-weight: 600;
            color: #64748b;
        }

        /* تحسين مظهر Select2 ليتناسب مع تصميمك */
        .select2-container--default .select2-selection--single {
            height: 48px;
            border: 1.5px solid #e2e8f0;
            border-radius: 8px;
            display: flex;
            align-items: center;
            padding: 0 10px;
        }
        .select2-container--default .select2-selection--single .select2-selection__arrow {
            height: 46px;
        }

        .form-abs-grid select, 
        .form-abs-grid input {
            width: 100%;
            padding: 12px 15px;
            border: 1.5px solid #e2e8f0;
            border-radius: 8px;
            font-size: 0.95rem;
            transition: all 0.2s ease;
            background: white;
        }

        .form-abs-grid select:focus, 
        .form-abs-grid input:focus {
            border-color: var(--accent-color);
            outline: none;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        /* تحسين الزر */
        .btn-submit-abs {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            height: 48px; /* توحيد الطول مع الحقول */
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-submit-abs:hover {
            background: #334155;
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        /* التجاوب مع الجوال */
        @media (max-width: 992px) {
            .form-abs-grid {
                grid-template-columns: 1fr;
            }
            .btn-submit-abs {
                width: 100%;
            }
        }
    </style>
</head>
<body>

<button class="sidebar-toggle" id="toggleSidebar"><i class="fas fa-bars"></i></button>

<div class="sidebar" id="sidebar">
    <div class="sidebar-header" style="padding: 20px; color: white; border-bottom: 1px solid rgba(255,255,255,0.1);">
        <h2><i class="fas fa-user-clock"></i> الاحتياط</h2>
    </div>
    <div class="sidebar-nav" style="padding: 10px;">
        <a href="/admin/dashboard" style="display: block; padding: 12px; color: white; text-decoration: none; border-radius: 8px; margin-bottom: 5px;">
            <i class="fas fa-home" style="margin-left: 10px;"></i> الرئيسية
        </a>
        <a href="/admin/settings" style="display: block; padding: 12px; color: white; text-decoration: none; border-radius: 8px;">
            <i class="fas fa-cog" style="margin-left: 10px;"></i> الإعدادات
        </a>
    </div>
</div>

<div class="main-content" id="mainContent">
    <header class="page-header">
        <h1>إدارة حصص الاحتياط الذكية</h1>
        <p>اليوم: <strong><%= today %></strong></p>
    </header>

   <% if (recapSubstitutions && recapSubstitutions.length > 0) { %>
    <section class="recap-banner">
        <h3 style="margin:0; color: var(--info-border); margin-bottom: 15px;">
            <i class="fas fa-check-circle"></i> حصص تم تغطيتها بنجاح
        </h3>
        <div class="recap-grid">
            <% recapSubstitutions.forEach(r => { %>
                <div class="recap-item">
                    <div style="display: flex; align-items: center; gap: 8px; flex: 1;">
                        
                        <a href="/admin/substitute/delete/<%= r.id %>" class="btn-retirer" title="إلغاء لإعادة التعيين" onclick="return confirm('إلغاء هذا التعيين وإعادة الحصة لقائمة الاحتياج؟')">
                            <i class="fas fa-times-circle" style="color: var(--danger-color);"></i>
                        </a>

                        <a href="/admin/substitute/delete/<%= r.id %>?reason=present" class="btn-retirer" title="المعلم حضر" onclick="return confirm('تأكيد حضور المعلم الغائب وإلغاء الاحتياط؟')">
                            <i class="fas fa-user-check" style="color: var(--success-color);"></i>
                        </a>

                        <div style="display: flex; flex-direction: column; margin-right: 5px;">
                            <span class="badge-periode">ح <%= r.periode %></span>
                            <span style="font-size: 0.85rem; margin-top: 4px; font-weight: bold;"><%= r.absent_name %></span>
                        </div>
                    </div>

                    <div class="arrow-icon">
                        <i class="fas fa-arrow-left"></i>
                    </div>

                    <div style="flex: 1; text-align: right;">
                        <strong style="color: var(--success-color); font-size: 0.95rem;"><%= r.substitute_name %></strong>
                        <small style="display:block; color: #64748b; font-size:0.75rem;"><%= r.classe %> - <%= r.section %></small>
                    </div>
                </div>
            <% }) %>
        </div>
    </section>
<% } %>

    <section class="card">
        <h3><i class="fas fa-user-plus"></i> تسجيل معلم غائب (يوم كامل)</h3>
        <form action="/admin/absences/ajouter" method="POST" class="form-abs-grid">
            <div class="input-group-custom" style="flex: 1;">
                <label>بحث بالاسم أو المادة:</label>
                <select name="enseignant_id" id="searchTeacher" required>
                    <option value="">-- اختر المعلم الغائب --</option>
                    <% enseignants.forEach(e => { %> 
                        <option value="<%= e.id %>"><%= e.nom %> (<%= e.matiere %>)</option> 
                    <% }) %>
                </select>
            </div>
            <div class="input-group-custom" style="flex: 1.5;">
                <label>سبب الغياب:</label>
                <input type="text" name="reason" placeholder="سبب الغياب (اختياري)...">
                <input type="hidden" name="date" value="<%= today %>">
            </div>
            <button type="submit" class="btn-submit-abs">تفعيل الغياب</button>
        </form>
    </section>

    <section class="card">
        <h3><i class="fas fa-chalkboard-teacher"></i> حصص تحتاج إلى بدلاء</h3>
        
        <% if (ghaibeen.length === 0) { %>
            <div style="text-align:center; padding:30px; color:#64748b;">
                <i class="fas fa-mug-hot fa-3x"></i>
                <p>كل الحصص مغطاة حالياً أو لا يوجد غيابات مسجلة.</p>
            </div>
        <% } else { %>
            <% ghaibeen.forEach(session => { %>
                <div class="absent-item-card">
                    <div class="session-info">
                        <span class="badge-periode">الحصة <%= session.periode %></span>
                        <div>
                            <strong style="font-size: 1.1rem;"><%= session.nom %></strong>
                            <span class="badge-subject" style="margin-right:10px;"><%= session.matiere %></span>
                        </div>
                        <div style="margin-right: auto; background: #f1f5f9; padding: 5px 12px; border-radius: 20px;">
                            <i class="fas fa-users"></i> القسم: <strong><%= session.classe %> - <%= session.section %></strong>
                        </div>
                    </div>

                    <div class="distribution-options">
                        <form action="/admin/substitute/assign-session" method="POST">
                            <input type="hidden" name="absent_id" value="<%= session.teacher_id %>">
                            <input type="hidden" name="periode" value="<%= session.periode %>">
                            <input type="hidden" name="classe" value="<%= session.classe %>">
                            <input type="hidden" name="section" value="<%= session.section %>">
                            <button type="submit" class="btn-auto">توزيع تلقائي</button>
                        </form>
                        <button class="btn-manual" onclick="toggleManual('m_<%= session.teacher_id %>_<%= session.periode %>')">اختيار يدوي</button>
                    </div>

                    <div id="m_<%= session.teacher_id %>_<%= session.periode %>" class="manual-suggestions-table">
                        <table style="width: 100%;">
                            <tbody>
                                <% suggestions.slice(0, 6).forEach(s => { %>
                                    <tr>
                                        <td><strong><%= s.nom %></strong> <small>(<%= s.reserve_this_month %> حصة هذا الشهر)</small></td>
                                        <td style="text-align: left;">
                                            <form action="/admin/substitute/assign-session" method="POST">
                                                <input type="hidden" name="substitute_id" value="<%= s.id %>">
                                                <input type="hidden" name="absent_id" value="<%= session.teacher_id %>">
                                                <input type="hidden" name="periode" value="<%= session.periode %>">
                                                <input type="hidden" name="classe" value="<%= session.classe %>">
                                                <input type="hidden" name="section" value="<%= session.section %>">
                                                <button type="submit" class="btn-submit-abs" style="padding: 4px 10px;">تعيين</button>
                                            </form>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            <% }) %>
        <% } %>
    </section>
</div>

<script>
    $(document).ready(function() {
        // تفعيل ميزة البحث السريع في القائمة المنسدلة
        $('#searchTeacher').select2({
            placeholder: '-- ابحث بالاسم أو المادة --',
            allowClear: true,
            dir: "rtl"
        });

        const toggleBtn = document.getElementById('toggleSidebar');
        const sidebar = document.getElementById('sidebar');
        const mainContent = document.getElementById('mainContent');

        if (toggleBtn) {
            toggleBtn.addEventListener('click', function() {
                sidebar.classList.toggle('collapsed');
                mainContent.classList.toggle('expanded');
                sidebar.classList.toggle('active');
            });
        }
    });

    function toggleManual(id) {
        const area = document.getElementById(id);
        if(area) {
            area.style.display = (area.style.display === 'table') ? 'none' : 'table';
        }
    }
</script>

</body>
</html>